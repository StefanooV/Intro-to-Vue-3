const app = Vue.createApp({
    data() {
        return {
            product: 'Socks',
            description: '100% cotton stockings. The best quality on the market, very good for sports and better than any other'
        }
    }
})
